# ContextSource 핵심 목표

## 비전

ContextSource는 소스 코드를 단순히 저장하거나 검색하는 도구가 아니다.

소스 코드로부터 구조적인 지식을 추출하여 사람과 AI가 동일한 관점으로 시스템을 이해할 수 있도록 하는 **Code Knowledge Platform**을 만드는 것이 목표이다.

---

## 핵심 목표

### 1. 코드를 구조적인 지식으로 변환한다.

소스 코드를 정적으로 분석하여 파일, 클래스, 함수와 같은 Entity와 이들 사이의 Relationship를 추출한다.

모든 분석 결과는 원본 코드(Evidence)와 연결되어 언제든지 근거를 확인할 수 있어야 한다.

---

### 2. 사람과 AI가 동일한 컨텍스트를 공유한다.

사람은 웹 UI를 통해 시스템 구조를 탐색하고, AI는 동일한 관계 데이터를 Query하여 질문에 필요한 정보만 활용한다.

모든 분석은 동일한 관계 모델을 기반으로 수행되며, 사람과 AI가 서로 다른 방식으로 코드를 이해하지 않도록 한다.

---

### 3. 시스템 구조를 빠르게 탐색한다.

파일 트리와 텍스트 검색이 아닌 관계 중심 탐색을 제공한다.

사용자는 다음과 같은 질문에 즉시 답을 얻을 수 있어야 한다.

* 누가 이 함수를 호출하는가?
* 이 클래스를 수정하면 어디까지 영향을 받는가?
* 특정 기능은 어떤 컴포넌트들로 구성되는가?
* 이 모듈은 어떤 시스템과 연결되어 있는가?

---

### 4. 반복되는 구현을 자산으로 축적한다.

프로젝트에서 반복적으로 등장하는 코드 구조와 설계 패턴을 식별하여 재사용 가능한 Pattern 또는 Component로 관리한다.

이를 통해 새로운 프로젝트에서도 검증된 구조를 빠르게 활용할 수 있도록 한다.

---

### 5. 개발 지식을 지속적으로 축적한다.

프로젝트를 분석할수록 ContextSource는 더 많은 구조와 패턴을 학습한다.

코드 자체를 저장하는 것이 아니라, 프로젝트에서 얻은 구조적 지식을 축적하여 개발 조직의 자산으로 발전시킨다.

---

## 궁극적인 목표

```text
Source Code
      │
      ▼
Static Analysis
      │
      ▼
Entity + Relationship + Evidence
      │
      ▼
Knowledge Graph
      │
      ├────────► Web Explorer
      │
      ├────────► AI Context
      │
      └────────► Pattern / Component Library
```

ContextSource의 최종 목표는 **코드 검색 도구**를 만드는 것이 아니라, **코드로부터 구조적인 지식을 생성하고, 이를 사람과 AI가 함께 활용하는 플랫폼**을 구축하는 것이다.
