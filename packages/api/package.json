{
  "name": "@contextsource/api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@contextsource/core": "*",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "vitest": "^2.0.5"
  }
}
